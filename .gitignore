node_modules
dist
.env
.env.local
.env.*.local
*.tsbuildinfo

# Claude Code project-level tooling config (per-clone, not shared)
/.claude/

# Lighthouse / Chrome DevTools report exports — generated locally, never
# checked in
*.report.html
*.report.json
lighthouse-report.*
chromewebdata_*
localhost_*.html
localhost_*.json
