import type { FoodPlanDocument as Doc } from '../lib/types'
export default function FoodPlanDocument(_props: { listId: string; userId: string; doc: Doc }) {
  return <p className="mt-6 text-sm text-gray-500">Food plan document (coming soon).</p>
}
